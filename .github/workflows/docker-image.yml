name: githubworkflow

on:
  push:
    branches: [ "main" ]
    

jobs:

  build:
    runs-on: ubuntu-latest
    environment: DATABASE
    steps:
    - uses: actions/checkout@v3
    - name: build and deploy project        
      run: |
        echo Password is ${{ secrets.DB_HOST }}
      # run: docker-compose -f "docker-compose.yml" up -d --build --force-recreate
